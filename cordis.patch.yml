# dsh-biz-bridge 插件补丁清单（随包发布的 bundle 层）
#
# 注意：本文件是 loader 补丁；新增插件行必须用 `- insert:` 列表（顶层
# `- id … name: …` 表示“覆盖已存在的行”，会因行不存在而报 entry not found）。
# inject 由源码声明，YAML 不重复声明。
#
# 开发形态（在源码目录直接接线）时，将 name 改为相对源码入口：
#   - insert:
#       - id: dsh-biz-bridge
#         name: './src/index.ts'
- insert:
    - id: dsh-biz-bridge
      name: 'dsh-biz-bridge'
